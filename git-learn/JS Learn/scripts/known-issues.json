{
  "$comment": [
    "Errors the audit finds, that are REAL, and that cannot be fixed yet.",
    "",
    "Why this file exists: the audit exited FAIL on the same three orphan tables",
    "every run for weeks. A permanently-red build is read exactly as often as a",
    "warning list that is 87% noise -- which is to say, never -- and then a NEW",
    "error lands in a red build nobody looks at. This course has lost two checks",
    "that way already.",
    "",
    "An entry here does NOT mean the problem is acceptable. It means it is known,",
    "it has a named gate, and the audit should stop shouting about it until that",
    "gate lifts. Unacknowledged errors still FAIL.",
    "",
    "The list is kept honest from BOTH ends:",
    "  - a new error that matches nothing here fails the audit",
    "  - an entry here that matches no current error ALSO fails the audit, as",
    "    stale. Otherwise this file becomes the place errors go to be forgotten,",
    "    which is the failure it was written to prevent.",
    "",
    "`match` is a plain substring of the error text. Keep it short enough to",
    "survive a recount -- match the table name, not 'queried in 2 lesson(s)'.",
    "This file is HAND-WRITTEN. PROGRESS.md is the generated one."
  ],
  "acknowledged": [
    {
      "match": "schema: table \"participants\"",
      "why": "Group/thread membership has no schema yet. Writing one now would bake in a shape that E2EE key distribution is about to change.",
      "gate": "B2 schema rewrite, itself behind C5 (E2EE)",
      "since": "2026-08-18"
    },
    {
      "match": "schema: table \"deletion_queue\"",
      "why": "DPDP erasure needs a retention policy decided first; the queue's columns follow from that, not the other way round. NARROWED 2026-08-21: b2/0002 settled the BULK half — messages are partitioned by month, so time-based retention is DROP TABLE on an old partition and needs no queue at all. What is still open is PER-USER erasure on request, which partitioning cannot serve because you cannot drop a partition for one person. That is the case the queue exists for, and it is the one still waiting on a policy: what must be erased, what may be kept as ciphertext nobody can read, and how long the queue is allowed to take.",
      "gate": "a per-user erasure policy, informed by b10/0002 (DPDP)",
      "since": "2026-08-18"
    },
    {
      "match": "schema: table \"calls\"",
      "why": "Call records depend on what WebRTC signalling actually needs to persist, which B6 has not settled.",
      "gate": "B2 schema rewrite, informed by B6 (signalling)",
      "since": "2026-08-18"
    }
  ]
}
